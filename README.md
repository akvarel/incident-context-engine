# Incident Context Engine

A deterministic, loss-aware context layer between observability backends and AI agents.
Raw telemetry remains in its source system. The engine emits compact incident snapshots with evidence references.
