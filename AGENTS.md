## Incident Context Engine

- Raw observability evidence is immutable and remains in the source backend.
- Every retained incident pattern must have a valid evidence reference.
- Redact sensitive data before any model-facing representation is produced.
- Deterministic behavior and tests are required for normalization, ranking, and budgets.
- Run `graphify update .` after code changes when `graphify-out/` exists.
